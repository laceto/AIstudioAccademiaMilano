# AI Chatbot — Streamlit + OpenAI

Generated by AI Studio Accademia Milano · Request 005

## Run locally

```bash
pip install -r requirements.txt

# Add your key
cp .streamlit/secrets.toml.example .streamlit/secrets.toml
# Edit secrets.toml and paste your OPENAI_API_KEY

streamlit run app.py
```

## Deploy to Streamlit Community Cloud

1. Push this folder to a GitHub repo
2. Go to https://share.streamlit.io → New app
3. Select the repo and set `app.py` as the main file
4. In **App settings → Secrets**, add:
   ```toml
   OPENAI_API_KEY = "sk-your-key-here"
   ```
5. Click Deploy — live in ~60 seconds

## Features

- ✅ User-configurable system prompt (sidebar, editable at runtime)
- ✅ Model selector (gpt-4o, gpt-4o-mini, gpt-3.5-turbo)
- ✅ Streaming responses token by token
- ✅ Full chat history in session
- ✅ Clear chat button
- ✅ API key via Streamlit Secrets — never hardcoded
- ✅ Graceful error if API key is missing
