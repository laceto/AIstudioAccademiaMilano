[phases.setup]
nixPkgs = ["python311", "libstdc++"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "pip install PyMuPDF>=1.24.0 || true"
]

[start]
cmd = "streamlit run streamlit_app.py --server.port $PORT --server.address 0.0.0.0 --server.headless true"
