# RAG team dependencies — kitai + FAISS + BM25 + LangChain
# pip install -r requirements-rag.txt

# kitai — batch embeddings, hybrid retriever, query translation
git+https://github.com/laceto/kitai.git

# Vector store
faiss-cpu==1.13.2
langchain-core==1.2.29
langchain-community==0.4.1
langchain-openai==1.1.13

# BM25 keyword retrieval (used by kitai.retriever)
rank-bm25==0.2.2

# OpenAI client
openai==2.31.0

# Env + data
python-dotenv>=1.0.0
numpy==2.3.4
pandas==2.3.3

# Offline fallback (local embedding without API key)
sentence-transformers>=2.7.0

# HTTP client (webhook outbound calls)
httpx==0.28.1
